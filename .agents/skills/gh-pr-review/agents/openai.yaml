interface:
  display_name: "Wind Studio Code Review"
  short_description: "Review code and architecture against project rules"
  default_prompt: "Use $gh-pr-review to review the current branch against Wind Studio code and architecture rules."
