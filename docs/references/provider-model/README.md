---
description: Home for provider and model reference docs covering the preset registry and its merge with user data
sources:
  - packages/provider-registry
  - src/main/data/services/ProviderRegistryService.ts
---

# Provider & Model Reference

How Cherry Studio defines AI providers and models: preset registry data ships in `packages/provider-registry`, is seeded into SQLite, and is merged with user configuration at runtime.

| Document | Purpose |
| --- | --- |
| [Provider & Model Registry System](./provider-registry.md) | How preset provider/model data is loaded, normalized, seeded, and merged with user data |
